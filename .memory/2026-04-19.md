## 32:00 [MCP-6] H7CAD PIDPATH 命令：A→B 最短路径

- **完成**：把图导航三件套补齐——PIDNEIGHBORS（看周围）+ PIDFIND（找对象）+ **PIDPATH（A→B 路径）**。命令族扩到 **14 个**。用户能用 `PIDPATH AAAA BBBB` 查任意两个对象之间通过哪些跳连接。
- **pid-parse 端 API 增量**（v0.4.1 polish 范围）：
  - `pub fn ObjectGraph::shortest_path<'a>(&'a self, from_id, to_id) -> Option<Vec<&'a str>>`
  - 实现：BFS + `predecessor: BTreeMap<&str, &str>` 记录前驱，找到 to_id 后反推
  - `from_id == to_id` → `Some(vec![from_id])`（零跳路径）；未知 endpoint 或不连通 → `None`
  - Time O(V+E), Space O(V)；循环安全（每个 drawing_id 入队一次）
  - 5 个新单测覆盖 zero-hop / direct neighbor / multi-hop / unreachable / unknown_endpoint
- **H7CAD 端 API 增量**：
  - `pub fn list_pid_path(source, from_or_prefix, to_or_prefix) -> Result<(PidNeighborInfo, PidNeighborInfo, Vec<PidNeighborInfo>), String>`
  - 内部公共 helper `resolve_drawing_id_in_graph(graph, input)` 抽出来——上一轮 `list_pid_neighbors` 内联的 prefix-match diagnostics 现在两个命令共享，错误信息（"ambiguous"/"no match"）跨命令一致
  - `list_pid_neighbors` 也重构调 `resolve_drawing_id_in_graph` 简化
- **命令实现**：`PIDPATH <from> <to>` 在 PIDSTATS 之后插入，要求恰好 2 个 positional token；输出格式：
  ```
  PIDPATH  3 hop(s) from AAAAAAAA… (Equipment) to CCCCCCCC… (Instrument)
      AAAAAAAA…  Equipment
      BBBBBBBB…  PipeRun
      CCCCCCCC…  Instrument  FIT-001
  ```
- **测试**：H7CAD `cargo test io::pid_import` **52/52 全绿**，3 个新增 PIDPATH 测试全过：
  - `list_pid_path_returns_path_through_chain`：A↔B↔C → A→C 返回 [A, B, C]
  - `list_pid_path_returns_error_when_no_path`：D 孤立 → A→D 错误含 "no path"
  - `list_pid_path_accepts_unique_prefix_for_both_endpoints`：两端都接前缀
- **PIDHELP 更新**：Graph 子组追加 PIDPATH，标题命令计数 13 → 14
- **构建**：pid-parse `cargo test --lib model::object_graph_impl_tests` 24/24；H7CAD `cargo build` 49s 全绿，无 lint
- **决策**：路径只返回对象序列，不返回边的 relationship guid（边 metadata 用户能从 path 邻接对再查）；无向看待（与 neighbors_of 一致）；`'a` 生命周期标注让返回 `Vec<&str>` 借用 graph 的 by_drawing_id key，免 String allocation
- **PID 命令族 14 个**：
  ```
  Write:    PIDSETDRAWNO, PIDSETPROP, PIDSETGENERAL
  Read:     PIDGETPROP, PIDGETGENERAL, PIDLISTPROPS
  Graph:    PIDNEIGHBORS [--depth N], PIDSTATS, PIDFIND, PIDPATH      ← +PIDPATH
  Integrity: PIDVERIFY, PIDSAVEAS, PIDRAWSTREAMS
  Help:     PIDHELP
  ```
- **未做**：k 条最短路径 / 加权边 / 方向化路径 / 路径上 relationship 详情 — 见 `docs/plans/2026-04-19-pidpath-cmd-plan.md` "不做"段
- **计划文档**：`docs/plans/2026-04-19-pidpath-cmd-plan.md`

## 31:30 [MCP-6] PIDNEIGHBORS --depth N：多跳邻居 BFS

- **完成**：把 PIDNEIGHBORS 从单跳升级为可指定 N 跳 BFS，支持"看这条管段一路连了什么设备" / "查这台仪表上下游 2 跳影响范围"等场景。`PIDNEIGHBORS AAAA --depth 3` 会列出 1..=3 跳之内所有可达对象。
- **pid-parse 端 API 增量**（v0.4.1 polish 范围）：
  - `pub fn ObjectGraph::neighbors_within(&self, drawing_id, depth) -> Vec<&PidObject>`
  - 语义：`depth=0` 空 Vec；`depth=1` 等价 `neighbors_of`；`depth=N` 累积 1..=N 跳所有 distinct 对象；BFS level-by-level，`BTreeSet<String>` 去重保循环安全
  - 5 个新单测：zero/one_equals_neighbors_of/two-hops/unreachable/cycle
- **H7CAD 端 helper 升级**：`list_pid_neighbors(source, drawing_id_or_prefix, depth: usize)` 加 depth 参数；底层从 `neighbors_of` 切到 `neighbors_within`。所有现有 5 处调用点统一加 `, 1` 保持原行为
- **命令分支重构**：从 `split_once(' ')` 单 token 解析升级为 `split_whitespace().enumerate()` token 遍历，支持 `--depth N` flag 任意位置；上限 N≤1000 防止误传巨数；未知 `--xxx` flag → push_error
- **测试**：H7CAD `cargo test io::pid_import` 含本批 2 个新增 (`with_depth_two_walks_two_hops` / `with_depth_zero_returns_only_self`) 全过；唯一失败测试是另一并行 layout 任务的，与本批无关
- **PIDHELP 文字更新**：`PIDNEIGHBORS <drawing-id-or-prefix> [--depth N]` + 描述加 "default depth=1"
- **输出格式微调**：depth=1 仍是 "N neighbor(s) of X (type)"；depth>1 加 "within K hop(s)"，例如 "3 neighbor(s) within 2 hop(s) of AAAAA8… (Equipment)"
- **构建**：pid-parse `cargo test --lib model::object_graph_impl_tests` 19/19；H7CAD `cargo build` 39.6s 全绿，无 lint
- **决策**：BFS 而非 DFS（level-by-level 输出顺序对用户友好）；起点不计入 neighbors（与 `neighbors_of` 一致）；depth ≤ 1000 上限是粗 sanity check（典型 P&ID 直径 < 50）；`BTreeSet<String>` 去重而非 `HashSet<&str>` —— 借用生命周期会与 `neighbors_of` 返回值打架，String 复制成本可接受
- **未做**：方向化遍历（仅沿 source→target）/ 路径返回（A→B→C 跳序列）/ 加权遍历 / `--include-self` flag — 见 `docs/plans/2026-04-19-pid-neighbors-depth-plan.md` "不做"段
- **计划文档**：`docs/plans/2026-04-19-pid-neighbors-depth-plan.md`

## 31:00 [MCP-6] H7CAD PIDFIND 命令：按 item_type 或 extra 字段搜对象

- **完成**：引入 `PIDFIND` 命令，把"找对象"的搜索维度补齐——和 PIDLISTPROPS（列属性）+ PIDNEIGHBORS（看邻居）形成完整查询三件套。命令族扩到 **13 个**。
- **CLI 双语法**：
  - `PIDFIND <item-type>` — 按 item_type 精确匹配（如 `PIDFIND PipeRun` / `PIDFIND Instrument`）
  - `PIDFIND <key>=<value>` — 按 extra 字段精确匹配（如 `PIDFIND Tag=FIT-001`）
  - `=` 切分由 `split_once('=')` 触发；空 key 或空 value → push_error
- **pid-parse 端 API 增量**（v0.4.1 polish 范围）：
  - `pub fn find_objects_by_item_type(&self, &str) -> Vec<&PidObject>` — 线性扫，源序返回
  - `pub fn find_objects_by_extra(&self, key, value) -> Vec<&PidObject>` — `extra.get(key) == Some(value)` 过滤
  - 4 个新单测覆盖匹配/未匹配/extra key 缺失/value 不匹配
- **H7CAD 端 API 增量**：
  - `pub enum PidFindCriterion { ItemType(String), ExtraEquals { key, value } }` — 命令层与 lib 层之间的封装类型
  - `pub fn list_pid_objects_matching(source, criterion) -> Result<Vec<PidNeighborInfo>, String>` — 复用 `PidNeighborInfo` 投影，与 PIDNEIGHBORS 输出格式一致
- **测试**：H7CAD `cargo test io::pid_import` **46/46 全绿**，3 个新增 PIDFIND 测试全过：
  - `list_pid_objects_matching_filters_by_item_type`：4 obj 含 2 PipeRun → ItemType("PipeRun") 返回 2
  - `list_pid_objects_matching_filters_by_extra_field`：3 obj 含 Tag="FIT-001"×2 + "FIT-002"×1 → ExtraEquals 返回 2
  - `list_pid_objects_matching_returns_empty_when_no_match`：无匹配返回空 Vec 而非错误
- **PIDHELP 更新**：Graph 子组追加 PIDFIND 两行说明；标题命令计数从 12 → 13
- **构建**：pid-parse `cargo test --lib model::object_graph_impl_tests` 14/14；H7CAD `cargo build` 51.6s 全绿，无 lint
- **决策**：精确匹配，不做正则/通配（第一版简洁明确）；不做多条件组合（AND/OR）等真实用例；`PidFindCriterion` enum 而非两个独立函数 → 命令层一个 helper 调用，可读性更好
- **PID 命令族 13 个**：
  ```
  Write:    PIDSETDRAWNO, PIDSETPROP, PIDSETGENERAL
  Read:     PIDGETPROP, PIDGETGENERAL, PIDLISTPROPS
  Graph:    PIDNEIGHBORS, PIDSTATS, PIDFIND                     ← +PIDFIND
  Integrity: PIDVERIFY, PIDSAVEAS, PIDRAWSTREAMS
  Help:     PIDHELP
  ```
- **计划文档**：`docs/plans/2026-04-19-pidfind-cmd-plan.md`

## 30:30 [MCP-6] drawing_id 前缀匹配：PIDNEIGHBORS 体验升级

- **完成**：把 PIDNEIGHBORS 的 drawing_id 输入从"硬记 32-hex"升级为"接受任何唯一前缀（≥1 字符）"。命令行体验从抄完整 GUID 简化为输 4-8 字符。
- **pid-parse 端 API 增量**（v0.4.1 polish 范围）：
  - `pub fn ObjectGraph::find_drawing_ids_by_prefix(&self, &str) -> Vec<&str>`：`BTreeMap::range(prefix..).take_while(starts_with)` O(log N + K)
  - 空 prefix 返回全部 id；长于实际 id 的 prefix 等价于精确未匹配
  - 4 个新单测：sorted_matches / empty_when_no_match / returns_multiple_when_unique_share_prefix / long_prefix_acts_as_exact_match
- **H7CAD 端 helper 升级**：
  - `list_pid_neighbors(source, drawing_id_or_prefix)` 解析顺序：精确 → 唯一前缀 → 0/N 匹配错误
  - 错误信息分三档：
    - 0 match → `no drawing_id matches '<X>' (exact or prefix)`
    - N≥2 match → `prefix '<X>' is ambiguous (matches N): id1, id2, id3, ... (M more)`（前 3 + 余数）
    - 1 match → 解析为该 id 走原邻居查询
  - 签名不变（向后兼容），现有精确 32-hex 调用继续 work
- **测试**：H7CAD `cargo test io::pid_import` **43/43 全绿**，3 个新增 + 1 旧测试断言更新（错误格式从"not found"改为"no drawing_id matches"）：
  - `list_pid_neighbors_accepts_unique_prefix`
  - `list_pid_neighbors_returns_ambiguous_prefix_error`
  - `list_pid_neighbors_returns_no_match_error_for_unknown_prefix`
- **PIDHELP 文字更新**：`PIDNEIGHBORS <drawing-id-or-prefix>` + 描述加 "unique prefix accepted"
- **构建**：pid-parse `cargo test --lib model::object_graph_impl_tests` 10/10；H7CAD `cargo build` 45.8s 全绿，无 lint
- **决策**：用 `BTreeMap::range` 而非 linear scan `objects`——前缀短、对象多时性能优；空 prefix 显式返回所有 id 让"列出全部"也能用同函数；错误信息预览前 3 个匹配，N>3 时加 "M more" 计数
- **不做**：大小写不敏感（drawing_id 按 SmartPlant 约定大写）/ PIDGETPROP/PIDSETPROP 也接前缀（与 attr 名前缀有歧义风险）/ 全局跨 object 前缀索引（BTreeMap 已足够）
- **计划文档**：`docs/plans/2026-04-19-drawing-id-prefix-match-plan.md`

## 30:00 [MCP-6] H7CAD PIDNEIGHBORS + PIDSTATS：消费 ObjectGraph API

- **完成**：把上一轮新加的 `ObjectGraph::neighbors_of` / `endpoint_resolution_stats` 接到 H7CAD 命令行。命令族扩到 **12 个**，作为 ObjectGraph API 的真实消费者验证设计。
- **两个新命令**：
  - `PIDNEIGHBORS <drawing-id>` — 列出某 drawing_id 对象的所有邻居（含自身 item_type / Tag），通过 `ObjectGraph::neighbors_of` 解析
  - `PIDSTATS` — 整 PID 一行汇总：N objects / M relationships + endpoint resolution F/P/U
- **H7CAD 层 API 增量**：
  - `PidNeighborInfo { drawing_id, item_type, model_id, tag_label }` — owned 投影；`tag_label` 取 `extra["Tag"]` → `extra["ItemTag"]` → 任一 extra 值 → None
  - `PidGraphStats { object_count, relationship_count, fully_resolved, partially_resolved, unresolved }`
  - `list_pid_neighbors(source, drawing_id) -> Result<(PidNeighborInfo, Vec<PidNeighborInfo>), String>` — 返回 `(self, neighbors)` 元组
  - `pid_graph_stats(source) -> Result<PidGraphStats, String>` — 整 graph 一行汇总
- **新 fixture helper** `cache_synthetic_graph_package`：手工构造 `PidPackage { parsed: PidDocument { object_graph: Some(...) } }` 直接 cache，绕过 cfb 真实解析，让 helpers 可独立单测
- **测试**：`cargo test io::pid_import` **40/40 全绿**，4 个新增全过：
  - `list_pid_neighbors_returns_self_and_resolved_neighbors`：3-obj/2-rel 合成图，验证 neighbors 顺序 + tag 提取
  - `list_pid_neighbors_returns_error_for_unknown_drawing_id`：未知 id → 错误信息含 "not found" + id
  - `pid_graph_stats_returns_aggregate_counts`：3-obj/3-rel(fully+partially+unresolved 各一) → 验证 stats 字段
  - `pid_graph_stats_without_object_graph_errors`：构造 object_graph=None 的 PidPackage，错误信息含 "no object_graph"
- **PIDHELP 更新**：新增 "Graph" 子组，列出 PIDNEIGHBORS / PIDSTATS；标题从 "PID metadata commands" 改为 "PID metadata + graph commands"
- **构建**：`cargo build` 56s 全绿，无 lint
- **验证 ObjectGraph API 设计**：上一轮抽出来的 `Vec<&PidObject>` 返回类型在 H7CAD 这一层很自然 —— `into_iter().map(project_neighbor).collect()` 一行就转 owned 投影，借用生命周期未成为障碍。`endpoint_resolution_stats()` 一次调用替代手写双段过滤，下游代码立刻收益
- **未做**：drawing_id 前缀匹配（第一版精确）/ 多跳邻居 `--depth N` / PIDSTATS JSON 模式
- **PID 命令族至此 12 个**（含 PIDHELP 自身）：
  ```
  Write:    PIDSETDRAWNO, PIDSETPROP, PIDSETGENERAL
  Read:     PIDGETPROP, PIDGETGENERAL, PIDLISTPROPS
  Graph:    PIDNEIGHBORS, PIDSTATS                      ← 新增
  Integrity: PIDVERIFY, PIDSAVEAS [--verify --force --dry-run], PIDRAWSTREAMS
  Help:     PIDHELP
  ```
- **计划文档**：`docs/plans/2026-04-19-pidneighbors-cmd-plan.md`

## 29:30 [MCP-6] pid-parse `ObjectGraph` 图遍历便利方法

- **完成**：在 `pid-parse::model::ObjectGraph` 上加一组 ergonomic 查询方法，让下游消费方（H7CAD、第三方工具）不必手动 `by_drawing_id.get(...).map(|i| &graph.objects[*i])` 链式调用，也不必自己写双段 `.filter().count()` 算解析率。
- **API 增量**（pid-parse v0.4.1 polish 范围）：
  - `pub fn object_by_drawing_id(&self, &str) -> Option<&PidObject>`：O(log N) 包装 by_drawing_id 索引
  - `pub fn relationships_touching(&self, &str) -> Vec<&PidRelationship>`：source/target 任一匹配 id 的关系
  - `pub fn neighbors_of(&self, &str) -> Vec<&PidObject>`：通过关系解析的对端对象，去重 + 跳过自环 + 跳过 None 端点
  - `pub fn endpoint_resolution_stats(&self) -> EndpointResolutionStats`：三态汇总
  - `pub struct EndpointResolutionStats { total, fully_resolved, partially_resolved, unresolved }`（Serialize/Deserialize/JsonSchema/Default 全套 derive）
- **重构**：`tests/parse_real_files.rs::relationship_endpoints_resolve_via_sheet_record` 从手写 `.iter().filter(s|s.is_some && t.is_some).count()` 双段简化为 `g.endpoint_resolution_stats()` 一次调用，断言语义不变但噪音少了一半。
- **测试**：pid-parse `cargo test --lib` **123/123 全绿**（115 → 123，新增 6 个 `object_graph_impl_tests` + 此前内部统计漏数的 2 个）；`cargo test --test parse_real_files` 28/28 全绿（27 → 28）；H7CAD `cargo build` 1m25s 全绿，下游兼容
- **设计取舍**：
  - `neighbors_of` 不引入 adjacency-list 缓存——第一版 O(R) 在样本规模（几百关系）足够，N×N 优化等真有性能数据再做
  - `BTreeSet<&str>` 做去重，迭代顺序确定可重现
  - 自环（source == target == drawing_id）显式过滤——和 SmartPlant 的实际 P&ID 拓扑期望一致
  - `EndpointResolutionStats` 加 `JsonSchema` derive 让 schema 端能继续序列化（与 `crossref::ClusterCoverage` 等同形）
- **未做**：路径搜索（A→B 多跳）/ adjacency-list 缓存 / `PidDocument` facade（先在 ObjectGraph 上加，将来真常用再升级）
- **不 bump 版本**：纯 additive lib API，无破坏性
- **计划文档**：`docs/plans/2026-04-19-object-graph-impl-plan.md`

## 29:00 [MCP-6] H7CAD PIDRAWSTREAMS 命令：消费新开放的 unidentified API

- **完成**：引入 `PIDRAWSTREAMS [<path>]` 命令，把上一轮开放的 `pid_parse::inspect::unidentified_top_level_streams` 接到 H7CAD 命令行。命令族扩展到 **10 个**，作为 pid-parse 新 API 的第一个真实消费者验证接口设计是否顺手。
- **用户价值**：开发者在调试 pid-parse 解码时能立即看到"当前 PID 样本里 pid-parse 没认出的顶层流"，不用去 scrape `pid_inspect` 报告文本；用户也能用它诊断某个 PID 的"未决内容有多少"
- **H7CAD 层 API 增量**：
  - `UnidentifiedStreamInfo { path, size, magic_u32_le, magic_tag }` — 拥有式投影（为 owned Clone+Eq，命令层与借用解耦）
  - `list_pid_unidentified_cached(source) -> Result<Vec<…>, String>` — 走 cached PidPackage
  - `list_pid_unidentified_file(path) -> Result<Vec<…>, String>` — 直接 parse 任意磁盘文件
  - 内部 helper `project_unidentified`：把 pid-parse 返回的 `Vec<&StreamEntry>` clone + `magic_tag` 复用 `pid_parse::parsers::magic::magic_tag`（pub 可用）得到 ASCII 显示
- **命令实现**：`PIDRAWSTREAMS` / `PIDRAWSTREAMS <path>` 在 `PIDVERIFY` 之后插入；输出格式对齐已有风格：
  ```
  PIDRAWSTREAMS  0 unidentified top-level stream(s) in cached <path>
  PIDRAWSTREAMS  2 unidentified top-level stream(s) in <target>
      /MysteryTop  19 B  magic=0x12345678
      /OtherMystery  512 B  magic=0x746F6F72 'root'
  ```
- **测试**：`cargo test io::pid_import` **32/32 全绿**，3 个新增全过：
  - `list_pid_unidentified_cached_returns_mystery_stream`：自造 fixture 含 `/MysteryTop` 根级流 + 正常 TaggedTxtData 流，断言只 MysteryTop 被列为 unidentified，magic 0x12345678 非 ASCII → magic_tag=None
  - `list_pid_unidentified_file_works_without_cache`：完全不经 cache 的端到端路径
  - `list_pid_unidentified_cached_without_cache_errors`：cache 缺失时的错误路径
- **新 fixture helper** `build_fixture_pid_with_toplevel_unknown`：在现有 Drawing/General 之上加 `/MysteryTop` 根级 stream，测试 `unidentified_top_level_streams` 正好命中它。filter 语义验证通过实际 cfb 创建流而非 mock。
- **PIDHELP 更新**：列表从 8 扩到 10（新增 PIDVERIFY/PIDRAWSTREAMS 之前 8 就已经提到了——其实 9，PIDHELP 自身不计入；现在实际用户可用命令是 **PIDSETDRAWNO/PIDSETPROP/PIDSETGENERAL/PIDGETPROP/PIDGETGENERAL/PIDLISTPROPS/PIDVERIFY/PIDSAVEAS/PIDRAWSTREAMS + PIDHELP = 10**）
- **构建**：`cargo build` 65s 全绿，无 lint
- **未做**：hex preview 在 PIDRAWSTREAMS 输出中内联（用 pid_inspect 覆盖这场景）；推导 magic 描述（有了就显示，没有就省）
- **决策**：命令层自定义 `UnidentifiedStreamInfo` 而非直接返回 lib 的 `&StreamEntry`：命令调度常用 owned Clone+Eq；接口设计验证通过——lib 侧 API 返回 `Vec<&StreamEntry>` 消费者能顺畅转成 owned，设计健壮
- **计划文档**：`docs/plans/2026-04-19-pidrawstreams-cmd-plan.md`

## 28:45 [MCP-6] pid-parse 解析层完善第 1 轮：可发现性 API + 条件测试

- **完成**：解析层两件互补事项——(1) 把 inspect 报告里私有的 "Top-level Unidentified Streams" 过滤逻辑抽成 pid-parse lib 公共 API，(2) `tests/parse_real_files.rs` + `tests/unit_parsers.rs` 里 27 个硬依赖真实 fixture 的测试改为优雅降级。
- **lib API 增量**（pid-parse，v0.4.1 polish 范围内）：
  - `pub const KNOWN_TOP_LEVEL_STREAM_NAMES: &[&str]`：13 个已识别顶层流名（`\x05SummaryInformation` / `PSMroots` / `DocVersion2` / `AppObject` / ...）
  - `pub const KNOWN_TOP_LEVEL_STORAGE_PREFIXES: &[&str]`：3 个已识别顶层存储前缀（`Sheet` / `TaggedTxtData` / `JSite`）
  - `pub fn unidentified_top_level_streams(&PidDocument) -> Vec<&StreamEntry>`：返回"当前 pid-parse 不识别的顶层流"——未来增量解码的待办清单 API
  - `inspect/report.rs` 内部的过滤块改用新 API（从 44 行缩到 1 行），人类输出一致
- **测试降级**：
  - `parse_test_file` 签名由 `-> PidDocument`（panic-on-fail）改为 `-> Option<PidDocument>`（fixture 缺失 `eprintln! SKIP + return None`）
  - 26 个 `let doc = parse_test_file(...)` 统一替换为 `let Some(doc) = parse_test_file(...) else { return };`
  - `unit_parsers::sheet_stream_reuses_cluster_header` 同样加 `!Path::new(path).exists() { eprintln!; return; }` 头部守卫
- **新测试**：
  - pid-parse `src/inspect/mod.rs::tests` 新增 4 个单元测：`unidentified_empty_for_default_doc` / `unidentified_filters_all_known_top_level_names` / `unidentified_keeps_unknown_top_level_entries` / `unidentified_strips_leading_slash_before_lookup`
  - `parse_real_files.rs` 追加条件集成测 `top_level_unidentified_streams_are_empty_on_sample_file`：fixture 存在时断言"零 unidentified"，为样本文件的识别进度建不变量
- **构建**：`cargo test` 全集 **115 lib + 27 parse_real + 7 writer_roundtrip + 1 writer_real + 8 writer_validate_cli + 18 unit_parsers + 10 module-内 = 全绿**。此前缺 fixture 的 27 个 FAILED 现在全部 `ok`（内部 early return）
- **决策**：常量用 `&[&str]` 而非 `&[&str; N]` 避免硬编码长度；lookup 用线性扫而非 HashSet（13 个元素，`.contains` 比 hash 更快）；路径规范化 `trim_start_matches('/')` 兼容 `/PSMroots` 和 `PSMroots` 两种写法
- **未做**：DocVersion2 结构化解码（无真实 fixture 难以验证）/ heuristic → decoded 升级（需要算法改进，另一个方向）/ H7CAD 端 `PIDRAWSTREAMS` 命令（先开放 API，等需求明确再加） — 见 `docs/plans/2026-04-19-pid-parse-discoverability-plan.md` "不做"段
- **计划文档**：`docs/plans/2026-04-19-pid-parse-discoverability-plan.md`

## 28:30 [MCP-6] PIDSAVEAS 扩展 --force / --dry-run

- **完成**：在 `PIDSAVEAS <path> [--verify]` 之上加两个实用 flag：
  - `--force` — 目标路径已存在时默认拒绝；加 `--force` 才覆盖。防止用户手滑覆盖原始 PID
  - `--dry-run` — 完全不触碰目标路径；写到 temp 后自动 verify，再清 temp。用于"改动后想确认能存但暂时不覆盖现文件"的探测
- **flag 解析升级**：从 `strip_suffix("--verify")` 的单 flag 识别升级为 token 遍历 match，支持任意顺序的 `--verify` / `--force` / `--dry-run`；未知 `--xxx` 立即 push_error 停止；非-- 的首个 token 作 path，额外 positional token 静默忽略
- **分支决策**：
  - `--dry-run` 走独立路径，走完即返回（即使带 `--force` 也忽略）
  - 正常路径：`out_path.exists() && !force_flag` → push_error "destination already exists; pass --force to overwrite"
  - 其它路径不变
- **报告**：
  ```
  PIDSAVEAS: destination 'existing.pid' already exists; pass --force to overwrite
  PIDSAVEAS  DRY-RUN  saved 4 stream(s) to <temp> (not persisted to <target>)
  PIDSAVEAS  DRY-RUN  PASS  4 streams matched
  PIDSAVEAS  DRY-RUN  FAIL  1 mismatch(es) (matched 3 of 4)
  ```
- **PIDHELP 更新**：PIDSAVEAS 行扩展为两行描述，覆盖新 flag
- **测试**：29/29 继续全绿。不为新 flag 写命令级测试（与其它 dispatch 分支一致）；底层 helper 路径未改，原有 `save_pid_native_then_verify_pid_file_always_passes` 仍覆盖核心流程
- **构建**：`cargo build` 30.5s 全绿，无 lint
- **决策**：`--dry-run` 总是写到进程 + nano 级唯一 temp，避免多实例并发冲突；`--dry-run --force` 静默吞掉 force（反正不落真盘）；额外 positional token 静默忽略（与 H7CAD 其它命令容忍风格一致）
- **计划文档**：`docs/plans/2026-04-19-pid-saveas-flags-plan.md`

### PID 命令族 9 个（PIDHELP 已更新）

## 28:15 [MCP-6] H7CAD PIDHELP 命令：PID 命令族的自助索引

- **完成**：加一条 `PIDHELP` 命令，在命令行内列出所有 9 个 PID 命令（含 PIDHELP 自身）及一行用法/描述，按 Write / Read / Integrity / Help 分组。解决 PID 命令族扩到 8 条后用户"记不清命令名"的可发现性问题。
- **实现**：`dispatch_command` 加 `cmd if cmd == "PIDHELP"` 分支，inline `push_output` + N 行 `push_info`，与 ALIASEDIT LIST / MLEADERSTYLE help 同风格。不抽 helper 函数（硬编码表即可，命令族同步成本低）
- **输出骨架**（缩进对齐）：
  ```
  PIDHELP  PID metadata commands (8 available)
      Write:
          PIDSETDRAWNO <new>                   shortcut for SP_DRAWINGNUMBER
          PIDSETPROP    <attr> <value...>      any Drawing-stream SP_* attribute
          PIDSETGENERAL <element> <value...>   General stream element text
      Read:
          PIDGETPROP    <attr>                 read Drawing attribute
          PIDGETGENERAL <element>              read General element text
          PIDLISTPROPS                         dump every Drawing attr + General element
      Integrity:
          PIDVERIFY [<path>]                   round-trip byte-level fidelity check
          PIDSAVEAS <path> [--verify]          save current PID + optional inline verify
      Notes:
          - All commands require an opened .pid file in the active tab.
          - Edits are metadata-only; native scene changes are not flushed.
  ```
- **构建**：`cargo build` 21.8s 全绿，无 lint；不写测试（纯 UI 字符串展示，无可 verify 的副作用，与其它 help 类命令风格一致）
- **决策**：不做 `PIDHELP <cmd>` 单命令详细说明（延迟到真的需要）；不做 `--cheatsheet` 输出 Markdown（延迟）；不做运行时反射发现（Rust 无 runtime reflection，硬编码 OK）
- **计划文档**：`docs/plans/2026-04-19-pidhelp-cmd-plan.md`

## 28:00 [MCP-6] H7CAD PIDSAVEAS 命令：专用 PID SaveAs + 可选内联 --verify

- **完成**：引入 `PIDSAVEAS <path> [--verify]` 把"另存 + 验证"原子化。此前用户要确认另存是否保真必须两步（SAVEAS X + PIDVERIFY X），现在一条命令完成：
  ```
  PIDSAVEAS new.pid             ← 仅保存
  PIDSAVEAS new.pid --verify    ← 保存后立刻对新文件跑 verify_pid_file
  ```
- **纯命令层改动**：底层 helper（`save_pid_native` / `verify_pid_file`）上一轮已就位，本轮只在 `commands.rs` 加一个分支 + 1 个组合测试。无新 API 增量。
- **参数解析**：`strip_suffix("--verify")` 识别后缀 flag；path 取剩余 trim；空 path / 非 .pid / active tab 无 PID source → push_error 明确 usage
- **输出格式**：
  - 成功：`PIDSAVEAS  saved 4 stream(s) to D:\out\x.pid`
  - +`--verify` PASS：追加 `PIDVERIFY  PASS  4 streams matched in D:\out\x.pid`
  - +`--verify` FAIL：追加 `PIDVERIFY  FAIL ... (matched X of Y)` + 前 3 条 mismatch 详情
- **测试**：H7CAD `io::pid_import` **29/29** 全绿，1 个新增 `save_pid_native_then_verify_pid_file_always_passes` 固定"save+verify pipeline 对 passthrough fixture 必 PASS"这个不变量。不为命令分支本身写测试（和其它 dispatch_command 分支一致，测试覆盖率由 helper 兜底）
- **行为**：保存成功后设 `tab.dirty = false`（与通用 SAVE 一致）；write_to 覆盖已有文件（当前行为），`--force` 提示留给下一轮
- **构建**：`cargo build` 30.9s 全绿，无 lint
- **决策**：专用命令而非 SAVE flag 内联——与 H7CAD 既有命令族风格一致（`PIDSETPROP` / `PIDGETPROP` / `PIDVERIFY` 都是 PID 专用前缀），`PIDSAVEAS` 把完整 PID 生命周期放到一个前缀树下，UX 一致
- **未做**：`--force` 覆写警告 / `PIDSAVEAS --dry-run`（写到 temp 只做 verify 不落盘）/ PIDHELP 命令列表 — 下一轮候选
- **计划文档**：`docs/plans/2026-04-19-pid-saveas-cmd-plan.md`

### PID 命令族至此完整（共 8 个）

| 命令 | 流/范围 | 操作 |
|---|---|---|
| `PIDSETDRAWNO <new>` | Drawing | 写 SP_DRAWINGNUMBER（便捷别名） |
| `PIDSETPROP <attr> <value...>` | Drawing | 写任意 SP_* 属性 |
| `PIDSETGENERAL <element> <value...>` | General | 写元素文本 |
| `PIDGETPROP <attr>` | Drawing | 读 |
| `PIDGETGENERAL <element>` | General | 读 |
| `PIDLISTPROPS` | Drawing+General | bulk 列出 |
| `PIDVERIFY [<path>]` | 整包 | round-trip 字节级保真验证 |
| `PIDSAVEAS <path> [--verify]` | 整包 | 另存 + 可选验证 |

## 27:30 [MCP-6] H7CAD PIDVERIFY 命令：在 GUI 内直接 round-trip 验证当前 PID

- **完成**：把"PID round-trip 字节级保真验证"从必须切到 CMD 跑 `pid_writer_validate`，简化为"在 H7CAD 命令行里输 `PIDVERIFY`"。两种用法：
  ```
  PIDVERIFY              ← 验证当前 active tab 的 cached PidPackage
  PIDVERIFY <path.pid>   ← 验证给定路径的 .pid 文件（不依赖 cache）
  ```
- **不抽 lib helper**：pid-parse `pid_writer_validate.rs` 在 binary module，pub 项目对外 crate 不可见；H7CAD 直接用 pid-parse 已有的 `parse_package` + `PidWriter::write_to` 自己写一份精简的 `compare_streams` 即可，避免为本命令膨胀 pid-parse 的 lib API surface（YAGNI；将来真有第三个 caller 再抽）
- **三层落地**：
  - `pid_import::PidVerifyMismatch { path, source_len, roundtrip_len, first_diff_offset }` + `PidVerifyReport { stream_count, matched, mismatches, only_in_source, only_in_roundtrip, ok() }`
  - `pid_import::verify_pid_cached(source) -> Result<PidVerifyReport, String>`：cached `Arc<PidPackage>` → `PidWriter::write_to(temp)` → `parse_package(temp)` → `compare_streams`，temp 文件自动 cleanup
  - `pid_import::verify_pid_file(path) -> Result<PidVerifyReport, String>`：完全不经 cache，对任意 .pid 文件做 round-trip
- **命令实现**：`commands.rs` 紧邻 `PIDLISTPROPS`，参数为空 → cached 路径；显式 path → 直接验证。输出格式：
  ```
  PASS: PIDVERIFY  PASS  4 streams matched in cached package <path>
  FAIL: PIDVERIFY  FAIL  1 mismatch(es) in <target> (matched X of Y)
            <stream-path>  source=A B  roundtrip=B B  first diff @ N
            ... (M more)            // 只显示前 3 条 mismatch
  ```
- **测试**：H7CAD `cargo test io::pid_import` **28/28 全绿**，3 个新增 verify_* 全过：
  - `verify_pid_cached_passes_for_unmodified_fixture`：load → verify_pid_cached → 4 streams matched
  - `verify_pid_cached_passes_after_metadata_edit`：edit_pid_drawing_attribute 后 cached 包仍能 round-trip 自身字节
  - `verify_pid_file_passes_for_synthetic_fixture_without_cache`：完全不经 cache 的端到端
- **构建**：`cargo build` 41.3s 全绿，无 lint
- **决策**：不在 SAVE 流里内联 verify（用户场景"先存再验证"两步式即可，比塞 flag 易读）；mismatch 只显示前 3 条避免命令行刷屏；temp 文件命名 `h7cad-pid-verify-{pid}-{nanos}-{tag}.pid`，cleanup 容错
- **未做**：mismatch 路径硬测试（writer 是确定性的，现实 mismatch 不会发生；mismatch 报告路径已被 pid-parse 的 `pid_writer_validate` 测试覆盖）/ 多文件批量 / SAVE-then-verify 内联 — 见 `docs/plans/2026-04-19-pid-verify-cmd-plan.md` "不做"段
- **计划文档**：`docs/plans/2026-04-19-pid-verify-cmd-plan.md`

## 27:00 [MCP-6] pid_writer_validate 加 --edit / --general-edit：编辑+验证一体化

- **完成**：把 `pid_writer_validate` 从只能"原样 round-trip"扩展到"模拟 metadata 编辑后再 round-trip 验证"。一个 CLI 跑完"编辑生效 + 其它流字节级保真"完整闭环：
  ```
  pid_writer_validate input.pid \
      --edit SP_DRAWINGNUMBER=NEW-001 \
      --edit SP_REVISION=2 \
      --general-edit FilePath=D:/issued/x.pid \
      --out edited.pid --keep --json
  ```
- **CLI**：新增 `--edit ATTR=VALUE` 和 `--general-edit ELEMENT=VALUE`，可重复多次。`splitn(2, '=')` 切分让 value 含 `=` 也工作。malformed 参数 → exit 1 + 提示"ATTR=VALUE"；attr 不存在 → exit 2 + 透传 `MetadataEditError` 信息
- **报告语义升级**：per-stream 三态
  - `OK` — 未编辑 + 字节相等
  - `EDITED` — 编辑触碰 + roundtrip 字节 == 应用 edit 后预期
  - `DIFF` — 意外差异（包含未编辑流字节变了 / 编辑结果与预期不符）

  `ok` 仍要求 0 个 DIFF；`ValidateReport` 新增 `edited` 计数 + `edits_applied: Vec<EditOp>` 字段，全部 `serde::Serialize`
- **测试**：`tests/writer_validate_cli.rs` 由 4 增至 **8 全绿**，新增 4 个：
  - `validate_with_edit_drawing_attribute_passes_and_marks_edited`：JSON 校验 `edited=1`/`matched=2`/`mismatched=0`，再验证写出文件确实含新值字节
  - `validate_with_general_edit_passes`：同上但走 General 流元素文本
  - `validate_edit_with_unknown_attr_exits_with_edit_error`：exit 2 + stderr 提到 `--edit SP_NOSUCH` 或 "not found"
  - `validate_edit_argument_malformed_exits_with_argument_error`：`--edit missing-equals-sign` → exit 1 + stderr 含 "ATTR=VALUE"
- **构建**：`cargo build --bin pid_writer_validate` 5.7s 全绿，无 lint，pid-parse v0.4.1 不 bump
- **决策**：以 **edited** 包（而非原始 source）做 round-trip 对照基准，让 EDITED 与 DIFF 语义清晰区分；多 edit 顺序按 args 顺序应用，同 attr 后值覆盖前值；BOM/UTF-16 与 v0.4.1 risk note 一致仍不处理
- **未做**：目录批量验证 / `--read-only` 跳过 write / namespace 深支持 / unified diff 格式 — 见 `docs/plans/2026-04-19-pid-writer-validate-edit-mode-plan.md` "显式不做"段
- **计划文档**：`docs/plans/2026-04-19-pid-writer-validate-edit-mode-plan.md`

## 26:30 [MCP-6] pid-parse 新增 `pid_writer_validate` CLI：真实 .pid round-trip 验证工具

- **完成**：在 `pid-parse` 仓库追加独立 binary `pid_writer_validate`，让用户能在不打开 H7CAD GUI 的情况下，对**任意真实 .pid** 跑 `parse_package → PidWriter::write_to (passthrough) → re-parse_package → per-stream byte diff` 全流程验证。这是 v0.4.0 落地报告里"对 SPPID 宿主完整兼容性需后续验证"的官方 CLI 工具。
- **CLI**：
  ```
  pid_writer_validate <input.pid> [--out <path>] [--keep] [--json] [--quiet] [--max-diff-bytes N]
  ```
  Exit 0=PASS / 1=mismatch / 2=parse-IO 错。Human format 输出"Reading source / Re-emitted / Re-parsing / 三段统计 / Result: PASS|FAIL"；`--json` 走 `serde_json::to_string_pretty` 给下游脚本消费。
- **公共 helper**（暴露在 binary 模块顶层供测试与未来集成用）：
  - `run_validate(input, output, max_diff_bytes) -> Result<ValidateReport, ValidateError>`
  - `ValidateReport { source_path, output_path, source_stream_count, roundtrip_stream_count, matched, mismatched, only_in_source, only_in_roundtrip, mismatches, ok }`
  - `StreamMismatch { path, source_len, roundtrip_len, first_diff_offset, source_window, roundtrip_window }`
  - 全部 `serde::Serialize`
- **测试**：`tests/writer_validate_cli.rs` **4/4** 全绿：
  - `validate_passes_on_synthetic_fixture_human_format`：内存 fixture（3 streams）→ 调 binary（spawn via `env!("CARGO_BIN_EXE_pid_writer_validate")`）→ 验证 exit 0 + stdout 含 "Result: PASS"
  - `validate_emits_json_when_flag_set`：`--json` 输出能被 `serde_json` 解析、`ok=true`、`matched=3`
  - `validate_help_flag_exits_zero_with_usage`
  - `validate_missing_input_exits_with_parse_error_code`：exit 2
- **不引新依赖**：用 cargo 自动设的 `CARGO_BIN_EXE_*` env 变量驱动 CLI，避免 `assert_cmd` / `escargot` 这类工具 crate
- **构建/全集**：lib 111/111、writer_roundtrip 7/7、writer_real_files 1/1、writer_validate_cli 4/4 全绿；`cargo build --bin pid_writer_validate` 2.6s 全绿、无 warning、无 lint
- **决策**：全部内存读 vs streaming：第一版直接 `Vec<u8>` 比对（典型 .pid < 100 MB，不必早优化）；diff 窗口居中对齐 first_diff_offset 抽 `max_diff_bytes` 字节；CFB 重建必有的整文件 diff（CLSID/时间戳）由报告明确说明只承诺"per-stream content equality"
- **未做**：目录批量验证（`<dir>` 递归）、`--edit SP_X=val` 模拟编辑路径再 validate、unified diff 格式 — 见 `docs/plans/2026-04-19-pid-writer-validate-cli-plan.md` "显式不做"段
- **计划文档**：`docs/plans/2026-04-19-pid-writer-validate-cli-plan.md`（注：该 plan 与 H7CAD `docs/plans/` 同栈风格一致，虽然 binary 落地在 pid-parse 仓）

## 26:00 [MCP-6] H7CAD PID metadata 读面对偶完整化：PIDGETGENERAL + PIDLISTPROPS

- **完成**：把 PID metadata 命令族的"读"一面补齐到与"写"对称。新增两个命令：
  - `PIDGETGENERAL <element>` — 读 General 流元素文本，PIDGETPROP 的对偶
  - `PIDLISTPROPS` — 一次列出 Drawing 全部 SP_* 属性 + General 全部 leaf-text 元素，对齐输出

  整体命令族表：

  | 命令 | 流 | 操作 |
  |---|---|---|
  | PIDSETDRAWNO / PIDSETPROP / PIDSETGENERAL | Drawing/General | 写 |
  | PIDGETPROP / PIDGETGENERAL | Drawing/General | 读单值 |
  | **PIDLISTPROPS** | Drawing+General | **bulk 读** |

- **三层落地**：
  - `pid-parse::writer::list_drawing_attributes(xml) -> Vec<(String,String)>`：byte-level 扫描每个开标签的 `name="value"` 对，按源序返回；跳过 PI/comment/CDATA 前缀；不强求支持 namespace prefix（确保不 crash 即可）
  - `pid-parse::writer::list_general_elements(xml) -> Vec<(String,String)>`：扫描所有 `<element>text</element>`，自闭合跳过；嵌套元素的外层跳过但内部 leaf 仍会被处理（实现关键：descend by skipping just the open tag，让 outer loop 接手内部）；CDATA 暂不深入
  - `H7CAD::pid_import::read_pid_general_element(source, element) -> Option<String>`：与 `read_pid_drawing_attribute` 对偶
  - `H7CAD::pid_import::list_pid_metadata(source) -> Result<PidPropsListing, String>`：典型化 Result（cache 缺失/stream 缺失/non-UTF-8 都明确报错）；`PidPropsListing { drawing_attributes, general_elements }`
- **命令实现**：`commands.rs` 在 `PIDSETGENERAL` 之后插入 `PIDGETGENERAL` 和 `PIDLISTPROPS` 两个分支
  - `PIDLISTPROPS` 输出：两个 `push_output` 段头 + N 行 `push_info` 缩进 attr/elem，列宽 `max(20, longest_key)` 自动对齐（参考 ALIASEDIT LIST 风格）
- **测试**：`pid-parse::writer::metadata_helpers` 由 24 增至 29 全绿（5 个新增 `list_*`）；H7CAD `cargo test io::pid_import` **25/25** 全绿，5 个本批新增（read_general / no_cache / list_pairs / no_cache / missing_general）全部 OK；之前并行任务的 `open_pid_merges_publish_sidecars_into_object_graph_when_present` 也修好了顺带过
- **构建**：H7CAD `cargo build` 33.5s 全绿，无 lint 错误，pid-parse 不 bump（v0.4.1 polish）
- **决策**：bulk 读用 byte-level 与 set/get 同质，避免引入 quick-xml round-trip 的归一化副作用；`list_general_elements` 关键 bug 修复——descend-by-just-open-tag 而非"遇 nested 整段 skip"，否则会丢失内层 leaf
- **未做**：PIDLISTPROPS 过滤参数（`DRAWING` / `GENERAL` / `<prefix>`）/ PID undo / SaveAs verify / XML namespace 深支持 — 见 `docs/plans/2026-04-19-pid-read-family-completion-plan.md` "显式不做"段
- **计划文档**：`docs/plans/2026-04-19-pid-read-family-completion-plan.md`

## 25:30 [MCP-6] H7CAD PID metadata 命令族完整化：PIDGETPROP + PIDSETGENERAL

- **完成**：把 PID metadata 命令从"只能写 Drawing 属性"扩展到"读+写 + Drawing/General 双流"，同一轮内交付 `PIDGETPROP <attr>` 和 `PIDSETGENERAL <element> <value...>` 两个新命令。组合命令族：

  | 命令 | 流 | 操作 |
  |---|---|---|
  | `PIDSETDRAWNO <new>` | Drawing | 写 SP_DRAWINGNUMBER（专用别名） |
  | `PIDSETPROP <attr> <value...>` | Drawing | 写任意 attr |
  | `PIDGETPROP <attr>` | Drawing | **读** attr 当前值 |
  | `PIDSETGENERAL <element> <value...>` | General | **写**元素文本 |

- **三层落地**：
  - `pid-parse::writer::get_general_element_text(xml, element) -> Option<String>`：对偶 `set_element_text`，自闭合 / 未找到 / 重复都返回 `None`；3 个新单测，writer::metadata_helpers 共 24 个全绿
  - `H7CAD::pid_import::edit_pid_general_element(source, element, value) -> Result<GeneralElementEdit, String>`：镜像 `edit_pid_drawing_attribute` 但作用于 `/TaggedTxtData/General` 流的 element 文本
  - `H7CAD::pid_import::read_pid_drawing_attribute(source, attr) -> Option<String>`：纯只读，缓存缺失/非 UTF-8/未找到/重复都 `None`（错误透明，UI 决定怎么展示）
- **命令注册**：`commands.rs` 在 `PIDSETPROP` 之后插入 `PIDGETPROP` 和 `PIDSETGENERAL` 两个分支，参数解析与错误处理风格与之前一致
- **测试**：H7CAD `cargo test io::pid_import` 17/18 通过，5 个本批新增全部绿色：
  - `edit_pid_general_element_replaces_file_path`：构造含 FilePath/FileSize/Author 的 General fixture，改 FilePath 后另两个 byte-level 保留
  - `edit_pid_general_element_returns_not_found_for_unknown_element`
  - `edit_pid_general_element_preserves_other_elements_byte_for_byte`：手工 String::replace 期望 vs 实际 helper 输出全等
  - `read_pid_drawing_attribute_returns_value_via_helper`：端到端 read 链路
  - `read_pid_drawing_attribute_returns_none_when_no_cache`：错误路径"软"返回 None
- **唯一失败** `open_pid_merges_publish_sidecars_into_object_graph_when_present`：另一个并行重构（"publish sidecars" feature，`pid_import.rs` 已扩到 1642+ 行）的测试，与本任务无关
- **构建**：H7CAD `cargo build` 43.9s 全绿，无 lint 错误
- **决策**：read 命令用 `Option<String>` 软返回（无典型化 Error），与 set 命令的 `Result<…, MetadataEditError>` 区分清晰；General 流不抽公共函数（schema 与 Drawing 不同，硬抽增加间接性而无收益）；不 bump pid-parse 版本（v0.4.1 polish 范围）
- **未做**：`PIDGETGENERAL` 读 General element / `PIDLISTPROPS` 列出所有属性 / `PID undo snapshot` / 写后 `--verify` 模式 — 见 `docs/plans/2026-04-19-pid-metadata-cmd-family-plan.md` "显式不做"段
- **计划文档**：`docs/plans/2026-04-19-pid-metadata-cmd-family-plan.md`

## 25:00 [MCP-6] H7CAD PIDSETPROP 命令：通用化 PID metadata 编辑

- **完成**：把上一轮"只能改 SP_DRAWINGNUMBER"升级为通用 `PIDSETPROP <attr> <value...>`，一次解锁全部 `SP_*` 属性（SP_PROJECTNUMBER / SP_DOC_NUMBER / SP_REVISION / SP_TITLE / …）。无需为每个属性新增专用命令。
- **重构**：H7CAD `pid_import` 引入通用 `edit_pid_drawing_attribute(source, attr, value) -> Result<DrawingAttributeEdit, String>`；旧 `edit_pid_drawing_number` 收编为通用版的薄包装（参数固定为 `"SP_DRAWINGNUMBER"`），`PIDSETDRAWNO` 命令面 + `DrawingNumberEdit` 类型完全向后兼容。
- **新命令**：`PIDSETPROP <attr> <value...>` 在 `dispatch_command` 紧邻 `PIDSETDRAWNO`：
  - 用 `cmd.splitn(3, ' ')` 分 [cmd, attr, value]，value 保留中间空格（不支持引号转义，与 H7CAD 命令行风格一致）
  - 空 attr / 空 value → push_error usage 提示
  - 错误透传 metadata_helpers 的 `MetadataEditError`（attr 拼错 → AttributeNotFound、属性出现多次 → DuplicateAttribute{count}）
  - 命令行回显格式：`PIDSETPROP  SP_PROJECTNUMBER 'PRJ-OLD' → 'PRJ-2026-A' (134 bytes Drawing XML; metadata-only edit)`
- **测试**：H7CAD `cargo test io::pid_import` **12/12** 通过，含本批 3 个新增：
  - `edit_pid_drawing_attribute_swaps_arbitrary_attribute`：fixture 含 SP_DRAWINGNUMBER+SP_PROJECTNUMBER+SP_REVISION，改 SP_PROJECTNUMBER 后另两个验证保留
  - `edit_pid_drawing_attribute_returns_attr_not_found_for_unknown_name`：`SP_NOSUCH` → 错误信息含 "metadata edit failed" + 属性名
  - `edit_pid_drawing_attribute_preserves_other_attributes_byte_for_byte`：构造期望 XML（手动 String::replace 单值）vs 实际 helper 输出，断言完全相等 — byte-level 保真验证
  - 上一轮 9 个测试（`edit_pid_drawing_number*` 等）继续全过 → 验证 thin-wrapper 重构未引入回归
- **构建**：H7CAD `cargo build` 56.1s 全绿，无 lint 错误，无新 warning。
- **决策**：保留 `PIDSETDRAWNO` 而非废弃 — `SP_DRAWINGNUMBER` 是日常高频属性，专用命令打字成本最低；`PIDSETPROP` 覆盖剩下的 long-tail。pid-parse 端**未改**（已具备 `set_drawing_attribute` 通用 API），不 bump 版本。
- **未做**：General 流元素文本命令 `PIDSETGENERAL <element> <value>`、读命令 `PIDGETPROP <attr>`、批量命令 `PIDSETPROPS k1=v1 k2=v2 …`、PID undo snapshot — 见 `docs/plans/2026-04-19-pid-setprop-generalization-plan.md` "显式不做"段。
- **计划文档**：`docs/plans/2026-04-19-pid-setprop-generalization-plan.md`

## 24:30 [MCP-6] H7CAD PIDSETDRAWNO 命令：第一个 PID metadata 编辑命令端到端跑通

- **完成**：在命令行落地 `PIDSETDRAWNO <new>`，端到端通路 `OPEN .pid → PIDSETDRAWNO X → SAVEAS *.pid` → 写出文件经 `parse_package` 验证 SP_DRAWINGNUMBER 已更新且其它流字节级保真。
- **三层落地**：
  - `pid-parse::writer::get_drawing_attribute(xml, attr) -> Option<String>`：read-only 配套，"exactly once" 语义（duplicates 返回 None，与 set 端拒绝重复对偶）；3 个新单元测试加在 v0.4.1 段（不 bump 0.4.2，属于 polish）
  - `H7CAD::pid_import::edit_pid_drawing_number(source, new_value) -> Result<DrawingNumberEdit, String>`：从 `pid_package_store` 取 `Arc<PidPackage>` → clone → splice `/TaggedTxtData/Drawing` UTF-8 → re-cache；`DrawingNumberEdit { previous: Option<String>, next: String, new_xml_len: usize }` 给 UI 报告用
  - `H7CAD::dispatch_command "PIDSETDRAWNO"` 分支（在 ALIASEDIT 之前）：参数校验 / `current_path.is_pid` 校验 / 调 helper / `command_line.push_output("PIDSETDRAWNO  '{prev}' → '{next}' ({n} bytes Drawing XML; metadata-only edit)")` / `tab.dirty = true`
- **测试**：H7CAD `cargo test io::pid_import` **9/9** 通过，含本批 4 个新增：
  - `edit_pid_drawing_number_swaps_attribute_in_cached_package`：fixture → load → edit → re-cache → 校验新值替换、旧值消失
  - `edit_pid_drawing_number_without_cached_package_errors`：错误信息含 "no cached PidPackage" + 源 path
  - `edit_pid_drawing_number_rejects_non_utf8_drawing_xml`：手工 cache 一个 UTF-16 BOM Drawing 流，错误信息含 "not UTF-8"
  - `edit_then_save_round_trips_new_drawing_number_through_disk`：edit → save → re-parse → 写出文件 Drawing 含新值 + General/Sheet/Blob 三个流字节级与原 fixture 相等
- **决策**：第一版"绕开 NativeCadDocument 的 metadata-only 编辑"——native scene 改动不回流，命令行回显里写明 `(metadata-only edit; native scene changes are not flushed)`；UTF-8 强约束，BOM/UTF-16 留给真实 fixture 出现后再做。
- **验证**：H7CAD `cargo build` 全绿（54.4s）；`cargo test io::pid_import` 9/9，未引入新 warning（既有 PidBrowser 中间态 warning 容忍）。
- **未做**：properties panel 编辑 UI / PID undo 栈 / 其它 SP_* 属性命令（PIDSETPROJECT 等）—— 见 `docs/plans/2026-04-19-pid-edit-cli-plan.md` "显式不做"段。下一迭代候选：(a) properties panel 暴露 SP_DRAWINGNUMBER 字段（用同一 helper）；(b) `PIDSETATTR <attr> <value>` 通用命令；(c) PID undo snapshot 机制
- **计划文档**：`docs/plans/2026-04-19-pid-edit-cli-plan.md`（与现存 H7CAD/docs/plans/ 风格一致）

## 23:55 [MCP-6] pid-parse v0.4.1：metadata 编辑 helper（为 H7CAD UI 编辑桥铺路）

- **完成**：在 `pid-parse` v0.4.0 Writer 之上新增纯函数 helper `src/writer/metadata_helpers.rs`，让上层不再需要自己拼/改 XML 字节即可改 `/TaggedTxtData/{Drawing,General}` 流。byte-level splice 实现，被替换的属性值/元素文本以外的字节（注释、空白、引号风格、兄弟属性顺序）逐字节保留。
- **公共 API**：
  - `set_drawing_attribute(xml, attr, value)`：`<Tag attr="…"/>` 风格属性值替换；`MY_ATTR` 不会误匹配 `EXTRA_MY_ATTR`
  - `set_drawing_number(xml, value)`：上一函数的 `SP_DRAWINGNUMBER` 别名
  - `set_element_text(xml, element, value)`：`<E>text</E>` 元素文本替换；自闭合标签拒绝
  - `set_general_file_path(xml, value)`：`FilePath` 优先 + `Path` 回退（与 `parsers/general_xml.rs` 接受面对齐）
  - `MetadataEditError`：`AttributeNotFound` / `DuplicateAttribute{count}` / `UnterminatedAttribute` / `ElementNotFound` / `DuplicateElement{count}` / `MalformedElement`，全部 `thiserror::Error + PartialEq`
- **XML 转义**：属性值转 `& < > " '`；元素文本转 `& < >`；调用方传裸字符串
- **测试**：`writer::metadata_helpers::tests` **18 个全绿**（基本替换、保留兄弟属性+空白、不匹配长名后缀、未找到、重复、XML 转义、空字符串、Unicode 中文+№、元素文本基本/带属性/自闭合拒绝/未找到/转义/不匹配长元素名后缀、`set_general_file_path` FilePath 优先 + Path 回退、空 XML 双类 NotFound）。`cargo test --lib` **100/100 通过**，writer_roundtrip 7/7、writer_real_files 1/1 全绿。`tests/parse_real_files.rs` 26 个 + `unit_parsers::sheet_stream_reuses_cluster_header` 失败与本次无关（依赖未提交的 `test-file/DWG-0201GP06-01.pid`）。
- **决策**：byte-level 而非 `quick_xml` round-trip，避免归一化引号风格 / 属性顺序 / 空白引入"我没改这里它怎么也变了"diff；不处理 BOM/UTF-16（v0.4.0 risk note 一致）；重复匹配显式拒绝而非 silently 替换第一个。
- **版本**：`pid-parse` 0.4.0 → 0.4.1。CHANGELOG.md 追加 0.4.1 段落。
- **下一步**：H7CAD 端可以直接把 `pid_parse::writer::set_drawing_number(xml, "新图号")` + `package.replace_stream("/TaggedTxtData/Drawing", new_xml.into_bytes())` 接到 properties 面板的 SP_DRAWINGNUMBER 字段提交流程上，无需在 H7CAD 拼 XML。

## 23:30 [MCP-6] H7CAD ↔ pid-parse Writer 接入：PID 文件 round-trip 保存通路打通

- **完成**：把 `pid-parse` v0.4.0 的 Writer (PidPackage + WritePlan + PidWriter) 接入 H7CAD 保存路径。打开 `.pid` → 编辑（暂时只是预览）→ "另存为 .pid"，输出文件按 stream 字节级与原文件对齐（passthrough 语义），CFB 容器由 `cfb_write::write_package` 重发。
- **新增**：
  - `src/io/pid_package_store.rs`：进程级 `Mutex<HashMap<PathBuf, Arc<PidPackage>>>` 缓存，path 用 `canonicalize` 归一化；API `cache_package` / `get_package` / `clear_package`；4 个并发安全单元测试（用唯一 path 防互相清表）
  - `src/io/pid_import.rs::save_pid_native(path, source_path)`：从缓存取出 `PidPackage` → `PidWriter::write_to(&pkg, &WritePlan::default(), path)`；缓存缺失时返回明确错误；缓存填充由 `open_pid` / `load_pid_native_with_package` 在打开时完成
  - `src/io/mod.rs`：`pick_save_path` 增加 `("PID Files (Smart P&ID)", &["pid"])` 过滤器；`save_native` 对 `.pid` 显式拒绝（`NativeCadDocument` 不持有原始 CFB 字节）并指引到 `pid_import::save_pid_native`
  - `src/app/helpers.rs::save_active_tab_to_path`：检测 `.pid` 后缀，从当前 tab 的 `current_path` 取 source_path 分发到 PID 写出路径
- **集成测试**：`pid_import::tests::save_pid_round_trips_every_stream_through_h7cad_save_path`：用 `cfb` crate 现造 4-stream fixture（Drawing/General/Sheet1/Blob）→ H7CAD 的 `load_pid_native_with_package` → `save_pid_native` → 再 `parse_package` → 按 path 比 `streams[…].data` 字节相等。`save_pid_without_prior_open_returns_explicit_error` 覆盖错误路径。
- **附带修复**（不可避免，否则 H7CAD bin 主分支编译都过不去）：
  - `src/app/update.rs::FileOpened` handler 适配新的 `crate::io::OpenFileResult { name, path, opened }` 解构，按 `OpenedDocument::Cad / Pid` 分支构造 `CadDocument` 与 `Option<NativeCadDocument>`
  - `src/app/update.rs::update`：补 7 个 `PidBrowser*` Message 变体的 stub no-op handler（PidBrowser feature 仍在另一并行重构中，先让 build 不阻塞）
  - `src/ui/pid_browser.rs`：`text(&item.title)` → `text(item.title.clone())`，修 iced 0.14 widget lifetime 借用问题
  - `src/io/pid_import.rs::PidOpenBundle`：去掉 `#[derive(PartialEq)]`（`PidDocument`/`nm::CadDocument` 都未实现 PartialEq）
- **决策**：第一版 **passthrough only**——UI 层对 `NativeCadDocument` 的修改不会回流到 PID 容器；下一迭代再做 metadata 编辑桥（`MetadataUpdates::drawing_xml` / `general_xml`）。PID 保存必须先在 H7CAD 中打开同 path 的 PID（缓存依赖），跨 path 另存第一版直接拒绝。CFB CLSID/时间戳不复刻（pid-parse v0.4.0 已知限制透传）。
- **结果**：`cargo build` 无 error（45.4s）；`cargo test io::` **36/36 通过**（含本次新增 `pid_package_store` 4 + `pid_import` round-trip 1 + 错误路径 1）。`cargo test` 全集 160/163 通过——失败的 3 个里 2 个属于本次新增（已在第二轮修复 `clear_all` 并发污染问题，重跑全绿），1 个 `prop_geom_commit_rejects_unsupported_native_hatch` 是 H7CAD 既有、与本次无关。
- **依赖**：`Cargo.toml` 加 `[dev-dependencies] cfb = "0.10"`，与 `pid-parse` 同版本。
- **下一步候选**：
  1. metadata 编辑桥：把 PID 标题/项目号等 `SP_*` 字段在 UI 暴露为可编辑，提交时构造 `WritePlan::metadata_updates.drawing_xml`，写出后保留其它 stream 字节
  2. SaveAs 路径校验：跨 path 另存时把源 PID 容器整体复制到目标 path 后再缓存，让 source_path 缓存依赖透明化
  3. PidPackage 缓存淘汰：tab 关闭时调 `clear_package(&path)` 防止长会话内存堆积
